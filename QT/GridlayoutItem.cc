
#include <QGridLayout>
QGridLayout *gridlayout = new QGridLayout;

for(int index =0;index < gridlayout->count();++index)
{
  QPushButton *pbutton = qobject_cast<QPushButton*>(gridlayout->itemAt(index)->widget()));

  if(pbutton != nullptr)
  {
  
    xxxxx;
  }

}
